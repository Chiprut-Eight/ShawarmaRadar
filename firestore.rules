rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Anyone can read restaurant rankings and data
    match /{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
